import { Icon } from "@/components/Icon";

export default function Page() {
  return (
    <main className="grid h-screen w-screen place-items-center">
      <Icon name="osnap-logo" className="aspect-[10/3] w-[50vw]" />
    </main>
  );
}
